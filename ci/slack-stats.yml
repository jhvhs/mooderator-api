platform: linux
image_resource:
  type: docker-image
  source:
    repository: bu380/mooderator-ci
    tag: v0.0.1

run:
  path: mooderator-api/ci/slack.sh

inputs:
  - name: mooderator-api

params:
  WEBHOOK_SUFFIX:
  API_URL:
  WEB_URL:

