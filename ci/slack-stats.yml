platform: linux
image_resource:
  type: docker-image
  source:
    repository: concourse/git-resource
    tag: latest

run:
  path: mooderator-api/ci/slack.sh

inputs:
  - name: mooderator-api

params:
  WEBHOOK_SUFFIX: ((webhook_suffix))


